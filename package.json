{
  "name": "form_builder",
  "private": true,
  "scripts": {
    "clean:dotfiles": "sh ./cleanDotFile.sh .",
    "build": "COPYFILE_DISABLE=1 bun run clean:dotfiles && turbo run build",
    "dev": "COPYFILE_DISABLE=1 bun run clean:dotfiles && turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.10.8",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "version": "1.3.5"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
